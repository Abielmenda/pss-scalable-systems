# Task division

## Person 1: mandatory final exercises

Responsible area: `final_exercises/`.

Tasks:

- Implement matrix functions: `transpose/1` and `matrixprod/2`.
- Implement tree functions: `tree_insert/2`, `inorder/1` and `map_tree/2`.
- Add tests for the mandatory exercises.
- Help review `Final.GenBank` and `Final.SuperBank`.

## Person 2: SPE project

Responsible area: `spe_project/`.

Tasks:

- Implement `SPE.start_link/1` and `SPE.stop/1`.
- Implement `SPE.submit_job/1` and `SPE.start_job/1`.
- Add the OTP supervisor and PubSub integration.
- Implement job parsing, task execution, timeouts and crash handling.
- Enforce the `:num_workers` limit.

## Shared work

- Copy Moodle tests without modifying them.
- Add extra tests.
- Complete README and AUTHORS.
- Prepare the presentation.
- Make sure both team members commit regularly.
