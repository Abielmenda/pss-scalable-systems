# SPE implementation modules

This folder will contain the internal modules for the SPE project:

- supervisor
- server
- job parser
- job runner
- task runner
- worker pool

The public API is expected to be exposed by module `SPE` in `lib/spe.ex`.
